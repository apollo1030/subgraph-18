{
  "name": "olympus",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --product hosted-service smartdev84/olympus",
    "create-local": "graph create --node http://localhost:8020/ smartdev84/olympus",
    "remove-local": "graph remove --node http://localhost:8020/ smartdev84/olympus",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 smartdev84/olympus"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.22.4",
    "@graphprotocol/graph-ts": "0.22.1",
    "mustache": "^3.1.0"
  }
}